.venv/
__pycache__/
*.pyc
.env
.pytest_cache/

# Google OAuth client + cached token (live credentials — never commit)
client_secret*.json
authorized_user*.json
secrets/

# macOS + local worker logs
.DS_Store
**/.DS_Store
logs/

# Raw email drops (LinkedIn alert samples) — contain PII/tracking tokens; never commit. Scrubbed fixtures live in tests/fixtures/.
/*.eml
